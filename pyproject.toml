[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bibcheck-verify"
version = "0.1.1"
description = "Deterministic bibliography verification for scientific papers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "bibtexparser>=1.4,<2",
  "httpx>=0.27,<1",
  "python-dotenv>=1.0,<2",
  "pymupdf>=1.24,<2",
  "rapidfuzz>=3.9,<4",
  "typer>=0.12,<1",
]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[project.scripts]
bibcheck-verify = "bibcheck.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/bibcheck"]

[tool.hatch.build.targets.wheel.force-include]
".github/skills/bibcheck-verify/SKILL.md" = "bibcheck/skill/SKILL.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
